import { prismaConnect } from "./prismaConnect";

export async function createSerial() {
  // const date = new Date();
  // let serial: string = "TLI";
  // serial =
  //   serial +
  //   date.toLocaleTimeString() +
  //   date.toLocaleDateString() +
  //   date.getSeconds().toString();
  // return serial.replace(/[.:\/pma\s]/g, "");
  // await prismaConnect();
  // const invoice = await prisma.invoice.findFirst({
  //   orderBy: {
  //     serial: 'desc',
  //   },
  // });
  //console.log("entro" + invoice)
}
